import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'keyboard-flickering',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
